class Recorder:
    pass
    # use terminate for thread joining
    # specify record folder
    # Wrap files into mp4 -- don't transcode
    # two threads -- one for folder management and wrapping, other for stopping/starting at 60sec
